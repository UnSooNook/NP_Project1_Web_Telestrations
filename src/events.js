// 이벤트 이름 저장소
const events = {
    // 닉네임 설정(나)
    setNickname: "setNickname",
    // 로그인 알리기
    newUser: "newUser",
    // 로그아웃(나)
    disconnect: "disconnect",
    // 로그아웃 알리기
    disconnected: "disconnected",
    // 메시지 보내기(나)
    sendMsg: "sendMsg",
    // 메시지 알리기
    newMsg: "newMsg"
};

export default events;